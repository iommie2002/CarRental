export const USCurrency = new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'USD',
});

export const INDCurrency = new Intl.NumberFormat('en-IN', {
    style: 'currency',
    currency: 'INR',
});

export const Currency = new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'ANG',
});